rrf:
  k: 60
  knn_k: 50
  ef_search: 128
  fetch_multiplier: 4

semantic:
  # Search mode for the semantic strategy.
  #   "knn" — pure k-NN vector search only (faster, no BM25 leg)
  #   "rrf" — hybrid Reciprocal Rank Fusion merging BM25 + k-NN (better precision)
  mode: "knn"

field_boosts:
  defaults:                 # SemanticSearchStrategy._DEFAULT_FIELD_BOOSTS
    name: 2.0
    domain: 1.0
    searchable_text: 1.0
    industry: 1.0
    locality: 1.0
  bm25_regular:             # RegularSearchStrategy _build_bm25_query field weights
    name: 2.0
    domain: 2.0
    searchable_text: 1.0
    industry: 1.0
    locality: 1.0
    name_phrase_boost: 10.0
    # Popularity boost: multiplies BM25 score by (1 + factor * log(1 + employee_count)).
    # Set to 0 to disable. Typical range: 0.1 – 0.5
    popularity_boost_factor: 2.0

cache:
  embedding_maxsize: 512
  classifier_maxsize: 256

embedding:
  # Sentence-transformers model name or local path.
  # Override with EMBEDDING_MODEL env var or by editing here.
  model: all-MiniLM-L6-v2
  # Vector dimension produced by the model.
  dimension: 384
  # Asymmetric retrieval prefix added to queries (not documents).
  # Set to empty string "" for symmetric models (e.g. all-MiniLM).
  query_prefix: "Represent this sentence for searching relevant passages: "

agentic:
  # Web-search backend for the agentic pipeline.
  #   "tavily"  — Tavily Search API (default, requires TAVILY_API_KEY)
  #   "serpapi" — SerpAPI Google AI Mode (requires SERPAPI_API_KEY)
  # If "serpapi" is selected without a SERPAPI_API_KEY, the pipeline logs a
  # warning at startup and falls back to Tavily. LinkedIn page extraction
  # always uses Tavily's /extract endpoint regardless of this setting.
  web_search:
    provider: "serpapi"

  # SerpAPI provider knobs (only used when web_search.provider == "serpapi").
  serpapi:
    # Slot 0 is reserved for the AI Mode summary, so 12 keeps ~11 citations.
    max_results: 12
    timeout_s: 10
    # Geolocation / language for the Google AI Mode query.
    gl: "us"
    hl: "en"
    # Optional location string (e.g. "Austin, Texas, United States"). null = unset.
    location: null

  # LLM model used for event extraction (deterministic pipeline).
  # gpt-4.1-mini: same price tier as gpt-4o-mini, ~30% faster output throughput
  # on structured JSON; handles json_object format reliably.
  extraction_model: "gpt-4.1-mini"
  # Maximum company names the LLM may return per extraction call
  max_company_names: 25
  # Maximum company results to return from the extraction prompt
  max_company_results: 12
  # Token budget for the LLM event-extraction call.
  # ~50-70 tokens per event; 600 covers ~10 events comfortably and forces
  # faster termination on edge cases.
  llm_max_tokens: 1200
  # OpenSearch candidates to fetch per resolved company name
  resolve_per_name: 5
  # Minimum OpenSearch score to accept a resolved company (avoids false matches)
  min_resolve_score: 0.5
  # Whether to resolve extracted company names against the OpenSearch index.
  # When true (default), each company name is matched via multi_match to enrich
  # results with index metadata (domain, industry, country, etc.).
  # When false, results are returned as synthetic docs with event data only —
  # useful to reduce latency or when the index is empty / not yet populated.
  resolve_to_index: false
  # Tavily web-search max results (only used when TAVILY_API_KEY is set)
  tavily_max_results: 6
  # Tavily search depth: "basic" (fast, shallow) or "advanced" (deeper, slower)
  tavily_search_depth: "basic"
  # Tavily HTTP request timeout in seconds
  tavily_timeout_s: 8
  # Redis TTL (seconds) for cached Tavily results in the fast pipeline.
  # News events don't change second-by-second — 5 minutes is safe for most queries.
  tavily_cache_ttl_s: 300
  # Character limit per result fed to the extraction LLM.
  # Used for Tavily results and as fallback when no AI summary is present.
  content_chars: 800
  # Character limit applied ONLY to the pinned "Google AI Mode summary" slot
  # (SerpAPI). The AI Mode body is dense and authoritative — give it room.
  summary_chars: 6000
  # Character limit per non-summary citation when an AI summary is present.
  # Citations are short fragments — keep this tight to control token usage.
  citation_chars: 400

  # LinkedIn enrichment branch (used when intent.named_companies is non-empty).
  linkedin:
    # Cap on number of named companies enriched in parallel per request.
    max_companies: 3
    # Max characters of LinkedIn page content fed to the extraction LLM.
    content_chars: 3000
    # Token budget for the LinkedIn extraction LLM call.
    llm_max_tokens: 600

  # Semantic prefetch fallback (when Tavily returns nothing).
  semantic_prefetch:
    k: 50
    size: 10
